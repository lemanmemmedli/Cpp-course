#include <iostream>
#include <string>

using namespace std;


/*
    Design Patterns

    Definition:
        Design patterns are common solutions to recurring design problems in software development.
        Think of them as templates or blueprints for solving specific types of problems.
        They provide a way to create flexible, efficient,
        and maintainable software by capturing best practices and proven solutions.

        Imagine you're building a house. You could design each house from scratch,
        but that would be time-consuming and error-prone.
        Instead, you might use common architectural patterns that have been proven to work well,
        like having a foundation, walls, and a roof. Similarly, in software development,
        design patterns help developers solve common problems in a reliable and efficient way.

        ~ There are different types of design patterns, such as creational, structural, and behavioral patterns.
        Creational patterns focus on object creation mechanisms,
        structural patterns deal with the composition of classes or objects,
        and behavioral patterns define how objects interact and communicate.

        For example, the Singleton pattern ensures that a class has only one instance and provides a global point of access to it.
        It's like having a single key to your house, ensuring there's only one instance of your home.

        By using design patterns, developers can save time, reduce errors, and create more maintainable and scalable software systems.


    Some well-known design patterns include:

    1. Singleton Pattern:
        Ensures that a class has only one instance and provides a global point of access to it.
        Factory Method Pattern:
        Defines an interface for creating an object, but leaves the choice of its type to the subclasses,
        creating an instance of one of the several possible classes.

    2. Abstract Factory Pattern:
        Provides an interface for creating families of related or dependent objects without specifying their concrete classes.

    3. Builder Pattern:
        Separates the construction of a complex object from its representation,
        allowing the same construction process to create various representations.

    4. Adapter Pattern:
        Allows the interface of an existing class to be used as another interface,
        converting one interface into another.

    5. Observer Pattern:
        Defines a one-to-many dependency between objects so that when one object changes state,
        all its dependents are notified and updated automatically.

    6. Decorator Pattern:
        Attaches additional responsibilities to an object dynamically.
        Decorators provide a flexible alternative to subclassing for extending functionality.

    5. Strategy Pattern:
        Defines a family of algorithms, encapsulates each one, and makes them interchangeable.
        Strategy lets the algorithm vary independently of clients that use it.

    6. Command Pattern:
        Encapsulates a request as an object, thereby allowing for parameterization of clients with different requests,
        queuing of requests, and logging of the parameters.

    7. Composite Pattern:
        Composes objects into tree structures to represent part-whole hierarchies.
        Composite lets clients treat individual objects and compositions of objects uniformly.

    8. State Pattern:
        Allows an object to alter its behavior when its internal state changes.
        The object will appear to change its class.

    9. Chain of Responsibility Pattern:
        Passes a request along a chain of handlers.
        Upon receiving a request, each handler decides either to process the request or to pass it to the next handler in the chain.


    Best Source: https://refactoring.guru/


    10. SOLID
        S.O.L.I.D stands for:
        S - Single-responsibility principle
        O - Open-closed principle
        L - Liskov substitution principle
        I - Interface segregation principle
        D - Dependency Inversion Principle

    Sources or SOLID principles:
        1) https://www.digitalocean.com/community/conceptual-articles/s-o-l-i-d-the-first-five-principles-of-object-oriented-design
        2) https://www.youtube.com/watch?v=zHiWqnTWsn4
*/


int main() {

    system("clear");

}
